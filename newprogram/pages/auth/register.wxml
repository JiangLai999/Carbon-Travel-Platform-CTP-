<view class="page">
  <view class="header">
    <view class="back-btn" bindtap="goLogin">
      <text class="back-icon">←</text>
    </view>
    <text class="header-title">创建账号</text>
    <view class="header-spacer"></view>
  </view>

  <view class="hero-section">
    <view class="hero-icon-wrap">
      <text class="hero-icon">🌱</text>
    </view>
    <text class="hero-title">加入低碳旅行</text>
    <text class="hero-subtitle">开启您的绿色出行之旅</text>
  </view>

  <view class="container">
    <view class="form-wrapper">
      <view class="form-group">
        <text class="form-label">手机号码</text>
        <view class="input-box">
          <text class="input-icon">📱</text>
          <input class="input-field" type="number" placeholder="请输入手机号" value="{{phone}}" bindinput="onInput" data-field="phone" placeholder-style="color: #d1d5db" maxlength="11" />
        </view>
      </view>

      <view class="form-group">
        <text class="form-label">验证码</text>
        <view class="input-box">
          <text class="input-icon">🔢</text>
          <input class="input-field" type="number" placeholder="请输入6位验证码" value="{{code}}" bindinput="onInput" data-field="code" placeholder-style="color: #d1d5db" maxlength="6" />
          <text class="code-btn {{countdown > 0 ? 'disabled' : ''}}" bindtap="sendCode">{{countdown > 0 ? countdown + 's' : '获取验证码'}}</text>
        </view>
      </view>

      <view class="form-group">
        <text class="form-label">设置密码</text>
        <view class="input-box">
          <text class="input-icon">🔐</text>
          <input class="input-field" type="password" placeholder="请输入密码（至少6位）" value="{{password}}" bindinput="onInput" data-field="password" placeholder-style="color: #d1d5db" />
        </view>
      </view>

      <view class="form-group">
        <text class="form-label">确认密码</text>
        <view class="input-box">
          <text class="input-icon">🔐</text>
          <input class="input-field" type="password" placeholder="请再次输入密码" value="{{confirmPassword}}" bindinput="onInput" data-field="confirmPassword" placeholder-style="color: #d1d5db" />
        </view>
      </view>

      <button class="btn-register" bindtap="handleRegister" loading="{{loading}}">
        <text class="btn-text">注册</text>
      </button>
    </view>

    <view class="agreement-tip">
      <text class="tip-text">注册即表示同意</text>
      <text class="tip-link">《用户协议》</text>
      <text class="tip-text">和</text>
      <text class="tip-link">《隐私政策》</text>
    </view>
  </view>
</view>
